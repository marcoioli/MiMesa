/** Placeholder owned by track C. This subtree must never import useEventStore. */
export default function GuestView() {
  return (
    <div className="mx-auto flex min-h-screen w-full max-w-[420px] flex-col gap-4 bg-ground px-5 py-8">
      <h1 className="text-[28px] font-extrabold leading-tight tracking-[-0.025em] text-ink">
        Vista del invitado (track C)
      </h1>
    </div>
  );
}
