/** Placeholder owned by track C; imported by the topbar (track A). */
export default function ShareButton() {
  return (
    <button
      type="button"
      disabled
      className="h-[34px] rounded-lg border border-line-2 bg-panel px-3 text-[13px] font-semibold text-ink disabled:opacity-50"
    >
      Compartir
    </button>
  );
}
