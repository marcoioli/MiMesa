# Tasks: MiMesa MVP (RF-01..RF-37)

**Status**: Phase 0 and Track B are complete and merged into `main` (2026-09-08). Tracks A and C are open; see `docs/tracks/track-a.md` and `docs/tracks/track-c.md`. Phase 4 runs after A and C merge.

No automated tests exist by explicit decision (`config.yaml` `strict_tdd: false`, requirements 2.2).
Every task's verification is a manual browser check under `npm run dev` plus `npx tsc --noEmit` and
`npm run build` passing. Size: **S** = 1 hour or less, **M** = 2-4 hours.

**Frozen files** (created in Phase 0, on `main`): everything under `src/app/**`, `src/store/**`,
`src/lib/**`, plus `src/features/tables/TableView.tsx`, `src/index.css`, `src/main.tsx`,
`vite.config.ts`, `tsconfig*.json`, `index.html`, `vercel.json`, `package.json`.
**Editing a frozen file after Phase 0 requires team agreement and a separate commit on `main`.**
Track branches MUST NOT touch frozen files or another track's files.

## Design references (added after the design round)

- `DESIGN.md` at the repo root is the visual source of truth: tokens, type scale, radii, control heights, component states. Every track implements against it.
- Mockups: `docs/design/*.dc.html` (published canvas "MiMesa Pantallas"). Artboard map: `CrearEvento` = track A (RF-01/04); `Main` = topbar + sidebar (track A) and canvas (track B); `Menus` = seat picker, seated-guest menu, table menu, add-table popover, confirm dialog, rejected drop (track B; the confirm dialog is frozen); `Compartir` = track C (RF-28/29); `Invitado` and `InvitadoResultado` = track C (RF-30..34).
- Phase 0 additions: the `src/index.css` task MUST declare the `DESIGN.md` tokens under Tailwind's `@theme` block, and `index.html` MUST load Manrope (500/600/700/800) from Google Fonts with the `"Segoe UI", system-ui, sans-serif` fallback. `TableView.tsx` MUST render the seat/disc/ring styles exactly as in `DESIGN.md` (both tracks reuse it).
- RF-32 copy was refined during the design round: caption "Estás en la mesa" plus the table name as a headline on its own line (see `specs/guest-view/spec.md`).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines (total, authored) | ~2,830 across 4 delivery units |
| Phase 0 (frozen base) | ~950 authored (+ generated `package-lock.json`, excluded from authored risk) |
| Track A (event + guests, 7 files) | ~600 (range 550-700) |
| Track B (canvas + DnD, 8 files) | ~745 (range 700-850) |
| Track C (share + guest view + export, 9 files) | ~535 (range 480-620) |
| 400-line budget risk | High (every unit exceeds 400) |
| Risk against this session's 800-line budget | Phase 0 High (exceeds); A Medium; B Medium; C Low |
| Chained PRs recommended | Yes |
| Suggested split | Base commit on `main` -> PR A / PR B / PR C in parallel (file-disjoint, any merge order) |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: size-exception
400-line budget risk: High

**Why "No"**: `proposal.md` section "Review Budget (single-pr, 800 lines)" already records an
**accepted `size:exception`** for the frozen base commit (greenfield scaffold, no prior behaviour to
regress). Tracks A, B and C are each forecast under the 800-line budget, so no further decision
blocks apply. Session `chain_strategy` is `none`: A/B/C are three independent parallel PRs to
`main`, not a stacked chain, because their file sets are disjoint.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 0 | Frozen base: scaffold, lib, store, app shell, `TableView`, placeholders | Base commit on `main` (`size:exception`) | `npx tsc --noEmit && npm run build` | `npm run dev`; `/` shows placeholder editor, `/invitado` shows placeholder guest view | Reset `main` to the pre-scaffold commit |
| A | Event lifecycle + guest list (RF-01..05, 13..19, 26) | PR A from `feat/track-a-event-guests` -> `main` | `npx tsc --noEmit && npm run build` | `npm run dev`; create event, bulk-load 20 guests, edit, reset | Delete branch; only `src/features/event/**` + `src/features/guests/**` revert |
| B | Canvas, tables, drag and drop (RF-06..12, 20..25) | PR B from `feat/track-b-canvas-dnd` -> `main` | `npx tsc --noEmit && npm run build` | `npm run dev`; drag a guest to a seat, to a table body, onto an occupied seat | Delete branch; only `src/features/tables/**` (minus frozen `TableView.tsx`) reverts |
| C | Share link, guest view, PNG export (RF-27..35) | PR C from `feat/track-c-share-guestview-export` -> `main` | `npx tsc --noEmit && npm run build` | `npm run dev`; copy link, open `/invitado#...` in a private window and on a phone | Delete branch; only `src/features/share/**`, `guest-view/**`, `export/**` revert |
| 4 | Post-merge integration smoke over RF-01..RF-35 | No PR (verification on `main` / Vercel) | `npx tsc --noEmit && npm run build` | Vercel production URL, desktop + 360px phone | N/A - verification only |

---

## Phase 0: Frozen base (built on `main` before anyone branches)

- [x] 0.1 Scaffold with `npm create vite@latest .` (React + TypeScript), choosing **"Ignore files and continue"** so `docs/` and `openspec/` survive; creates `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`. **Done when**: `npm run dev` serves the Vite starter page. (M)
- [x] 0.2 Install exact versions: `npm i react-router-dom@7.18.3 zustand@5.0.15 @dnd-kit/core@6.3.1 lz-string@1.5.0 qrcode.react@4.2.0 html-to-image@1.11.13` and `npm i -D tailwindcss@4 @tailwindcss/vite@4.3.3`; record them in `package.json`. **Done when**: install completes with no `--legacy-peer-deps` and no peer warnings for `@dnd-kit/core`. (S)
- [x] 0.3 Wire Tailwind v4: add `tailwindcss()` to the plugin array in `vite.config.ts` and replace `src/index.css` with `@import "tailwindcss";` plus the `DESIGN.md` tokens under `@theme static` and a Manrope `font-sans` body rule (the RF-35 export uses `skipFonts` and falls back to Segoe UI, accepted). **Done when**: a `class="text-emerald-600"` element renders green in the browser. (S)
- [x] 0.4 Create `vercel.json` with a single rewrite of `/(.*)` to `/index.html` so `/invitado` resolves on a hard reload (SPA deploy). **Done when**: the file parses as JSON and `npm run build` still passes. (S)
- [x] 0.5 RF-05, RF-11: create `src/lib/constants.ts` with `CANVAS_W=1600`, `CANVAS_H=1200`, `GRID_COLS=5`, `GRID_PITCH_X=290`, `GRID_PITCH_Y=270`, `GRID_MARGIN=50`, `MIN_CAPACITY=2`, `MAX_CAPACITY=20`, `MAX_TABLES=20`, `QR_MAX_URL=1200`, `CANVAS_NODE_ID='mimesa-canvas'`, `EXPORT_IGNORE_ATTR='data-export-ignore'`, `TEMPLATE_PRESETS` (6x8, 10x10, 8x6). **Done when**: `npx tsc --noEmit` passes. (S)
- [x] 0.6 RF-14, RF-16: create `src/lib/ids.ts` exporting a collision-free `newId()` (`crypto.randomUUID()` with a counter fallback). **Done when**: two calls in the console return different strings. (S)
- [x] 0.7 RF-18, RF-31, RF-35: create `src/lib/text.ts` with `normalize` (trim, lowercase, NFD, strip `̀-ͯ`) and `slugify` (non-alphanumeric runs to `-`, fallback `'evento'`). **Done when**: in the console `normalize('José ')==='jose'` and `slugify('Casamiento Ana y Juan')==='casamiento-ana-y-juan'`. (S)
- [x] 0.8 RF-05, RF-09, RF-11: create `src/lib/geometry.ts` with `SEAT=28`, `seatRadius`, `tableRadius`, `tableBox`, `seatAngle`, `seatOffset`, `seatPositions`, `gridLayout` (5x4) and `clampToCanvas`, exactly as in design.md. **Done when**: `tableBox(8)===120`, `tableBox(20)===258`, and `gridLayout(19)` fits inside 1600x1200. (S)
- [x] 0.9 RF-20..RF-23: create `src/lib/dnd.ts` with the `GuestDragData` / `TableDragData` / `SeatDropData` / `TableDropData` types and the `guestDragId`, `tableDragId`, `seatDropId`, `tableDropId` id builders. **Done when**: `npx tsc --noEmit` passes; this is the contract tracks A and B compile against. (S)
- [x] 0.10 Create `src/store/types.ts` with `Guest`, `Table` (`seats: (string|null)[]`), `Event`, `TableTemplate` and `SharePayload` (`v:1; n; d; p?; t:{n;s}[]`). **Done when**: `npx tsc --noEmit` passes. (S)
- [x] 0.11 RF-01..RF-27: create `src/store/useEventStore.ts` with `persist` (`name:'mimesa-event'`, `version:1`, `partialize` to `{event}`, `migrate` returning `{event:null}`) and **all 16 actions** implementing the exact semantics table in design.md: `createEvent`, `updateEvent`, `resetEvent`, `addTable`, `removeTable`, `renameTable`, `setTableCapacity`, `moveTable`, `clearTable`, `clearAllTables`, `addGuests`, `addGuest` (returns `string|null`), `removeGuest`, `seatGuest` (returns `boolean`), `unseatGuest`, `autoSeat` (returns `{seated,leftover}`). **Done when**: from the console `createEvent('X','2026-11-20',undefined,{count:6,capacity:8})` yields 6 tables `Mesa 1..6` and localStorage key `mimesa-event` appears. (M)
- [x] 0.12 RF-15, RF-19, RF-32: create `src/store/selectors.ts` with pure `seatedGuestIds`, `unseatedGuests`, `guestNamesById`, `seatNamesOf`, `occupancyOf` and `counters` (`freeSeats = sum(capacity) - seated`). **Done when**: with 20 guests, 3 tables of 8 and 5 seated, `counters` returns `{total:20,seated:5,unseated:15,freeSeats:19}`. (S)
- [x] 0.13 Create `src/app/router.tsx` (`/` -> `EditorPage`, `/invitado` -> `GuestViewPage`, `*` -> redirect `/`), `src/app/EditorPage.tsx` (`event ? <AppLayout/> : <EventForm/>`), `src/app/GuestViewPage.tsx` (renders `<GuestView/>`, with the mandatory comment: never import `useEventStore` here), and point `src/main.tsx` at the router. **Done when**: `/` and `/invitado` both render and a hard reload of `/invitado` works in dev. (M)
- [x] 0.14 Create `src/app/AppLayout.tsx`: `<DndProvider>` wrapping `<Topbar/>` above a two-column `[<GuestSidebar/> | <CanvasArea/>]`, plus `<ToastHost/>`. **Done when**: with an event created, `/` shows the topbar, sidebar and canvas placeholders side by side. (S)
- [x] 0.15 RF-03, RF-06, RF-13, RF-17: create `src/app/ConfirmDialog.tsx` with the frozen API `{open, message, confirmLabel?, onConfirm, onCancel}`, Spanish buttons `Cancelar` / `Confirmar`, and `data-export-ignore="true"` on its root. **Done when**: a temporary trigger opens and closes it; `window.confirm` is never used anywhere. (S)
- [x] 0.16 RF-22, RF-26: create `src/app/useToastStore.ts` (non-persisted zustand exposing `toast(message)`) and `src/app/Toast.tsx` (`ToastHost`, auto-dismiss ~3s, `data-export-ignore="true"`). **Done when**: calling `toast('hola')` from the console shows and auto-hides a notice. (S)
- [x] 0.17 RF-20, RF-23: create `src/app/dnd/GuestDraggable.tsx`, a `useDraggable` wrapper taking `{guestId, from, children}` that emits `GuestDragData` with `guestDragId(guestId)` and sets `style={{touchAction:'none'}}`. **Done when**: `npx tsc --noEmit` passes; both A (sidebar cards) and B (seated guests) can use it without editing each other's files. (S)
- [x] 0.18 RF-09, RF-10, RF-32: create the frozen `src/features/tables/TableView.tsx` implementing `TableViewProps` exactly (`name`, `capacity`, `seatNames`, `highlightSeatIndex`, `rejectedSeatIndex`, `occupancyTone`, `onSeatClick`, `renderSeat`, `renderBody`, `renderCenter`, `className`): relative root sized `tableBox(capacity)`, absolutely positioned `28x28` seat wrappers, truncated centre name with a `title` attribute plus `5/8` occupancy, tones slate / amber / emerald. **Done when**: rendering it with capacity 20 shows 20 non-overlapping seats and the centre label truncated with a working tooltip. (M)
- [x] 0.19 Create zero-prop, default-exported compiling placeholders so both routes build before any track starts: `src/features/event/Topbar.tsx`, `src/features/event/EventForm.tsx`, `src/features/guests/GuestSidebar.tsx`, `src/features/tables/CanvasArea.tsx`, `src/features/tables/DndProvider.tsx` (children pass-through), `src/features/share/ShareButton.tsx`, `src/features/export/ExportButton.tsx`, `src/features/guest-view/GuestView.tsx`. **Done when**: `npm run build` passes and no track needs to create a file another track imports. (S)
- [x] 0.20 **Base acceptance checklist** - all four MUST pass before branching: (a) `npx tsc --noEmit` and `npm run build` succeed; (b) `/` shows the placeholder editor (topbar + sidebar + canvas); (c) `/invitado` shows the placeholder guest view; (d) after `useEventStore.getState().createEvent('Demo','2026-11-20',undefined,{count:6,capacity:8})` in the console, localStorage key `mimesa-event` exists and survives a reload. **Done when**: all four pass; then commit to `main` and create the three track branches from it. (S)

---

## Track A: Event and guests - branch `feat/track-a-event-guests`

Owns only `src/features/event/**` and `src/features/guests/**`. RF-01..05, RF-13..19, RF-26.
M-priority tasks (A.1-A.6) come before S-priority tasks (A.7-A.11).

- [x] A.1 RF-01, RF-04, RF-05: implement `src/features/event/EventForm.tsx` - fields `Nombre del evento`, `Fecha`, `Lugar (opcional)`, the four templates `6 mesas de 8` / `10 mesas de 10` / `8 mesas de 6` / `Personalizado` (with `Cantidad de mesas` 1-20 and `Capacidad` 2-20), submit `Crear evento`, calling `createEvent`. Validation messages: `Completá el nombre y la fecha.` and `Ingresá entre 1 y 20 mesas y una capacidad entre 2 y 20.` **Done when**: submitting name+date+"6 mesas de 8" replaces the form with the editor and the canvas shows `Mesa 1`..`Mesa 6`; empty name or date shows the message and creates nothing. (M)
- [x] A.2 RF-15: implement `src/features/guests/GuestSidebar.tsx` and `src/features/guests/GuestCard.tsx` - list `unseatedGuests(event)` as cards showing the name, each card wrapped in the frozen `src/app/dnd/GuestDraggable.tsx` (read-only) with `from: null`. **Done when**: with 10 guests and 3 seated, exactly 7 cards are listed and the list updates the instant a guest is seated. (M)
- [x] A.3 RF-14: implement `src/features/guests/BulkAddGuests.tsx` - textarea labelled `Un nombre por renglón` plus an `Aceptar` button calling `addGuests(text)`, clearing the textarea on success. **Done when**: pasting `"  Ana  \n\n Juan\n\n  "` creates exactly `Ana` and `Juan`, and `"Ana\nAna"` creates two distinct cards. (S)
- [x] A.4 RF-16: implement `src/features/guests/AddGuestInline.tsx` - a single input plus `Agregar invitado` calling `addGuest(name)`; a whitespace-only value creates nothing and leaves the input usable. **Done when**: typing `Pedro` and confirming adds one card; confirming an empty value adds none and shows no blocking error. (S)
- [x] A.5 RF-03, RF-19 shell: implement `src/features/event/Topbar.tsx` - event name/date/place, buttons `Nuevo evento` (opens the frozen `ConfirmDialog` with `¿Crear un evento nuevo? Se borrará el evento actual.` then `resetEvent()`), and import the zero-prop `ShareButton` from `src/features/share/ShareButton.tsx` (read-only) and `ExportButton` from `src/features/export/ExportButton.tsx` (read-only). **Done when**: confirming shows the creation form again; cancelling leaves the event untouched. (M)
- [x] A.6 RF-02: implement `src/features/event/EventEditDialog.tsx` and open it from `src/features/event/Topbar.tsx` - edits name, date and place through `updateEvent`; clearing the place is valid. **Done when**: after editing, the topbar shows the new values and every table position, seat assignment and guest is unchanged. (S)
- [x] A.7 RF-19: add the four counters to `src/features/event/Topbar.tsx` using `counters(event)`: `Invitados`, `Sentados`, `Sin ubicar`, `Sillas libres`. **Done when**: 20 guests / 3 tables of 8 / 5 seated shows 20, 5, 15, 19; reducing one table to capacity 6 without displacing anyone shows free seats 17. (S)
- [ ] A.8 RF-18: add the `Buscar invitado` field to `src/features/guests/GuestSidebar.tsx`, filtering with `normalize` from `src/lib/text.ts` (read-only); keep the query in component state, never in the store. **Done when**: typing `jose` lists `José Pérez` and `Josefina` only, and clearing it restores the full list. (S)
- [ ] A.9 RF-17 (sidebar half): add a delete control to `src/features/guests/GuestCard.tsx` that opens `ConfirmDialog` with `¿Eliminar a este invitado?` and calls `removeGuest`. **Done when**: confirming removes the card and decrements the counters; cancelling changes nothing. (S)
- [ ] A.10 RF-13: add `Vaciar todas las mesas` to `src/features/event/Topbar.tsx`, confirming with `¿Vaciar todas las mesas? Los invitados vuelven al panel.` then calling `clearAllTables`. **Done when**: every table shows zero occupancy, every guest is back in the sidebar, and table count, names, capacities and positions are unchanged. (S)
- [ ] A.11 RF-26: add `Autoubicar` to `src/features/event/Topbar.tsx` calling `autoSeat()` and toasting `Se ubicaron N invitados.` plus, when `leftover > 0`, `Quedaron N invitados sin ubicar por falta de sillas.` **Done when**: with 10 unseated guests and 4 free seats the first 4 in sidebar order are seated, 6 remain, and the notice appears even when a search filter is active. (S)

---

## Track B: Canvas, tables and drag and drop - branch `feat/track-b-canvas-dnd`

Owns only `src/features/tables/**` **except the frozen `TableView.tsx`**. RF-06..12, RF-20..25.
M-priority tasks (B.1-B.6) come before S-priority tasks (B.7-B.12).

- [x] B.1 RF-05, RF-09: implement `src/features/tables/CanvasArea.tsx` (scrolling container with a fixed `1600x1200` **inner** node carrying `id={CANVAS_NODE_ID}` - never on the scroller, or RF-35 loses off-screen tables) and `src/features/tables/TableNode.tsx` (absolutely positioned at `table.x/table.y`, rendering the frozen `src/features/tables/TableView.tsx` (read-only) with `seatNamesOf`). **Done when**: a 10-table event shows tables 1-5 in the first row and 6-10 in the second, the canvas scrolls instead of resizing, and each centre reads e.g. `Mesa 1` / `0/8`. (M)
- [x] B.2 RF-20, RF-21: implement `src/features/tables/DndProvider.tsx` - `DndContext` with `PointerSensor` (`activationConstraint: {distance: 5}`), the composed collision detection from design.md (`pointerWithin` then `closestCenter`, seat hits beating table hits, empty array for `type==='table'`, container read from `collision.data.droppableContainer`), an `onDragEnd` switch calling `seatGuest`, and a minimal `DragOverlay` guest chip carrying `data-export-ignore="true"`; make each seat a droppable via `renderSeat` (wrapper `w-full h-full`) and the disc a droppable via `renderBody` in `src/features/tables/TableNode.tsx`. **Done when**: dragging a sidebar guest onto seat 3 of `Mesa 2` seats them there and removes the card; dropping on the table body of a table with seats 0-1 taken seats them in seat 2. (M)
- [x] B.3 RF-22: add rejection feedback in `src/features/tables/DndProvider.tsx` and `src/features/tables/TableNode.tsx` - `onDragOver` marks an occupied seat (passed as `rejectedSeatIndex`) or a full table (red ring applied through the `renderBody` slot, since `TableViewProps` has no full-table flag); on a rejected `onDragEnd` flash the ring ~600ms and toast `Esa silla ya está ocupada.` or `La mesa está completa.` **Done when**: dragging over an occupied seat shows a red ring, and on release the seat is unchanged, the guest is back in the sidebar and the notice appears. (M)
- [x] B.4 RF-24, RF-17 (seat half): implement `src/features/tables/SeatedGuestMenu.tsx`, opened by clicking a seated guest in `src/features/tables/TableNode.tsx`, offering `Quitar de la mesa` (`unseatGuest`) and `Eliminar invitado` (`ConfirmDialog` with `¿Eliminar a este invitado?` then `removeGuest`); mark the menu `data-export-ignore="true"`. **Done when**: a press-and-release under 5px opens the menu with no seat change, and `Quitar de la mesa` empties the seat and returns the guest to the sidebar. (M)
- [x] B.5 RF-25: implement `src/features/tables/SeatPicker.tsx`, opened by `onSeatClick` on an empty seat - lists `unseatedGuests` with its own search field, a `Nuevo invitado` field with a `Sentar` action (`addGuest` then `seatGuest` into the clicked seat), and the `Sentar invitado` title; mark it `data-export-ignore="true"`. **Done when**: searching `ped` and picking `Pedro` seats him in the clicked seat and closes the picker; typing `Lucía` in `Nuevo invitado` seats her immediately without her ever appearing in the sidebar. (M)
- [x] B.6 RF-06: implement `src/features/tables/AddTableDialog.tsx` (`Agregar mesa`, capacity 2-20, `addTable`, toasting `Máximo 20 mesas.` when already at 20) and `src/features/tables/TableMenu.tsx` with `Eliminar mesa` confirming `¿Eliminar la mesa? Sus invitados vuelven al panel.` then `removeTable`. **Done when**: adding a table of capacity 10 to a 6-table event puts a seventh table at the next grid slot; deleting an occupied table returns its guests to the sidebar with their names intact and deletes no guest. (M)
- [x] B.7 RF-23: wrap each seated guest in `src/features/tables/TableNode.tsx` with the frozen `src/app/dnd/GuestDraggable.tsx` (read-only), passing `from: {tableId, seatIndex}`. **Done when**: dragging a guest from seat 1 of `Mesa 1` to empty seat 4 of `Mesa 5` frees seat 1 in the same operation, creates no duplicate and leaves the counters unchanged; a rejected reseat keeps the origin seat. (S)
- [x] B.8 RF-10: confirm the occupancy tone reaches `TableView` from `src/features/tables/TableNode.tsx` (derived from `seatNames` when `occupancyTone` is omitted). **Done when**: an empty capacity-4 table is slate, turns amber on the first guest, emerald when full, and back to slate when emptied. (S)
- [x] B.9 RF-07, RF-12: add `Capacidad` (2-20, `setTableCapacity`) and `Vaciar mesa` (`clearTable`) to `src/features/tables/TableMenu.tsx`. **Done when**: raising a capacity-6 table to 10 keeps guests in seats 0 and 3; lowering a capacity-8 table with a guest in seat 7 to 6 returns that guest to the sidebar; `Vaciar mesa` on `Mesa 4` empties only `Mesa 4`. (S)
- [x] B.10 RF-08: implement `src/features/tables/TableNameEdit.tsx`, opened by double-clicking the centre label (`renderCenter` slot in `src/features/tables/TableNode.tsx`), committing through `renameTable`. **Done when**: double-clicking `Mesa 2`, typing `Mesa novios` and confirming renames it without moving it, and an empty name keeps the previous one. (S)
- [x] B.11 RF-11: make the table draggable - `useDraggable` with `TableDragData` on the **centre label only** in `src/features/tables/TableNode.tsx` (never the whole disc, or a pointerdown on a seated guest would bubble), and handle `type==='table'` in `src/features/tables/DndProvider.tsx` with `moveTable(id, x+delta.x, y+delta.y)`. **Done when**: dragging the label 200px right moves the table and the position survives a reload; dragging past the edge clamps it fully inside 1600x1200; a press-and-release under 5px does not move it. (M)
- [x] B.12 RF-09 stress check: render a capacity-20 table with a long name. **Done when**: the 20 seats stay legible and non-overlapping, the name truncates inside the disc, and hovering reveals the full text via the `title` tooltip. (S)

- [x] B.13 RF-06 (placement): move the `Agregar mesa` button out of the scrolled canvas so it is always visible at the bottom-right of the canvas area (overlay on the scroller's parent, `data-export-ignore="true"`). **Done when**: the button is visible without scrolling at any zoom and any scroll position. (S)
- [x] B.14 RF-36: canvas zoom in `src/features/tables/CanvasArea.tsx` — component state `zoom` (0.5..2, step 0.1, default 1), `-` / `100 %` / `+` controls bottom-right next to `Agregar mesa` (the label resets to 100% on click), Ctrl + wheel zooms around the pointer; the inner node keeps 1600x1200 with `transform: scale(zoom)` and `transform-origin: 0 0`, wrapped in a sizing box of `1600*zoom x 1200*zoom` so scrolling covers the scaled canvas. Table drags divide the pointer delta by `zoom` before `moveTable`, and the live translate of a dragged table divides `transform.x/y` by `zoom`. Zoom is never persisted and the PNG export is unaffected (it captures the unscaled node; track C may need to neutralise the transform on capture — note it in apply-progress). **Done when**: at 200% a 200px on-screen drag moves a table 100px on the canvas; the zoom label and scrollbars update; reload returns to 100%. (M)
- [x] B.15 RF-37: table selection and size in `src/features/tables/TableNode.tsx` and `src/features/tables/TableMenu.tsx` — a click on the disc (not a drag, not a seat) selects the table (accent 2px outline + soft halo through the `renderBody` wrapper); Escape or a click on the canvas background deselects (selection state lives in `CanvasArea` and is passed down); the selected table shows a small floating toolbar above its box (`data-export-ignore`) with `Tamaño` `-` / `+`, and `TableMenu` gets the same `Tamaño` stepper; both call `setTableScale(tableId, scale ± TABLE_SCALE_STEP)` within `MIN_TABLE_SCALE..MAX_TABLE_SCALE` and disable the button at the bound. `TableNode` applies `transform: scale(table.scale ?? 1)` with `transform-origin: 0 0` to the `TableView` wrapper (so the box grows from its top-left corner; seats and text scale with it) and sizes its outer box to `tableBox(capacity) * scale` so droppable rects stay correct. **Done when**: selecting "Mesa 3" and pressing `+` twice draws it at 1.5x with the same guests and "5/8", it survives a reload, `+` disables at 2x, `-` at 0.75x, and drops onto its seats still work at 1.5x. (M)

---

## Track C: Share, guest view and export - branch `feat/track-c-share-guestview-export`

Owns only `src/features/share/**`, `src/features/guest-view/**`, `src/features/export/**`.
RF-27..35 (RF-27 lands in the frozen base; C verifies it).
M-priority tasks (C.1-C.7) come before S-priority tasks (C.8-C.9).

- [ ] C.1 RF-27 (verification only, no file edits): with an event of 8 tables (one renamed, one moved), 40 guests and 25 seated, reload the page. **Done when**: name, date, place, table names, capacities, positions, guests and seat assignments are identical; a browser with nothing stored shows the creation form; after `Nuevo evento` a reload still shows the form; hand-editing the stored `version` to 99 shows the creation form instead of crashing. (S)
- [ ] C.2 RF-28: implement `src/features/share/buildSharePayload.ts` (`{v:1, n, d, ...(place?{p:place}:{}), t: tables.map(t => ({n: t.name, s: seatNamesOf(event, t)}))}`) and `src/features/share/encode.ts` (`buildShareUrl` via `compressToEncodedURIComponent`, `readSharePayload` via `decompressFromEncodedURIComponent` with a 100,000-char cap, `try/catch`, and `v===1 && Array.isArray(t)` validation). **Never** `encodeURIComponent` / `decodeURIComponent` / `URLSearchParams` on the fragment - add the warning comment from design.md. **Done when**: a payload containing `+` and `$` round-trips through `buildShareUrl` -> `readSharePayload` unchanged. (M)
- [ ] C.3 RF-28: implement `src/features/share/ShareButton.tsx` (zero-prop, default export, label `Compartir` - its file already exists as a placeholder imported by `Topbar`) and `src/features/share/ShareDialog.tsx` with `Copiar link` and the `¡Link copiado!` confirmation; mark the dialog `data-export-ignore="true"`. **Done when**: clicking copy puts `<origin>/invitado#<payload>` on the clipboard and shows the Spanish confirmation. (M)
- [ ] C.4 RF-30: implement `src/features/guest-view/GuestView.tsx` - read `window.location.hash` raw, decode via `readSharePayload`, render the event name, date, place when present and the `Buscá tu nombre` field; on a missing, empty, truncated or unknown-version fragment render `El link no es válido. Pedile uno nuevo al organizador.` instead of the search box. **Never import `useEventStore` here.** **Done when**: a valid link shows the header and the field; `/invitado` with no fragment, with garbage, and with `v:2` all show the invalid-link message without crashing. (M)
- [ ] C.5 RF-31: implement `src/features/guest-view/GuestSearch.tsx` - build `(name, tableName, seatIndex)` tuples from `payload.t` and match with `normalize(name).includes(normalize(query))` from `src/lib/text.ts` (read-only). **Done when**: typing `jose` offers `José Pérez`; two guests named `Ana` at `Mesa 1` seat 0 and `Mesa 4` seat 3 are listed as two distinct results, each showing its table name. (M)
- [ ] C.6 RF-32, RF-33: implement `src/features/guest-view/GuestResult.tsx` - render the caption `Estás en la mesa` with the table name as a headline on its own line (see `docs/design/InvitadoResultado.dc.html`) and the frozen `src/features/tables/TableView.tsx` (read-only) with **no slots** (read-only drawing, zero dnd-kit) passing `seatNames` and `highlightSeatIndex` from the chosen tuple; with no match render exactly `No encontramos tu nombre. Consultá al organizador.` and no table drawing. **Done when**: choosing the `Mesa 1` occurrence highlights only seat 0 of `Mesa 1`; searching an unseated or absent name shows the not-found message alone. (M)
- [ ] C.7 RF-34: make `src/features/guest-view/GuestView.tsx`, `GuestSearch.tsx` and `GuestResult.tsx` fit a 360px viewport - single column, tap targets at least 44px, table drawing scaled or horizontally centred without page-level horizontal scrolling. **Done when**: at 360x640 in device emulation the header, field, result list and table drawing are readable, a match is selectable by tap, and the page never scrolls sideways. (M)
- [ ] C.8 RF-29: add the QR to `src/features/share/ShareDialog.tsx` - render `<QRCodeCanvas value={url} size={256} level="L" />` **only** when `url.length <= QR_MAX_URL` (1,200); otherwise render the copy button plus `El evento es muy grande para generar un QR. Copiá el link y compartilo.` Never wrap the QR in `try/catch` - it throws during render. **Done when**: a ~60-guest event shows a scannable QR, a ~200-guest event shows copy + note with no crash, and copying works identically in both states. (S)
- [ ] C.9 RF-35: implement `src/features/export/exportCanvas.ts` (`toPng` on `document.getElementById(CANVAS_NODE_ID)` after `await document.fonts.ready`, with `pixelRatio: 2`, `backgroundColor: '#ffffff'`, `cacheBust: true`, `skipFonts: true`, a `filter` dropping `data-export-ignore="true"` nodes, and the documented one-shot retry when `dataUrl.length < 5000`) and `src/features/export/ExportButton.tsx` (zero-prop, default export, label `Exportar PNG`, downloads `plano-${slugify(event.name)}.png`). **Done when**: exporting `Casamiento Ana y Juan` downloads `plano-casamiento-ana-y-juan.png` containing every table including off-screen ones, on opaque white, with any open menu absent. (M)

---

## Phase 4: Integration (after A, B and C merge into `main`)

- [ ] 4.1 Merge the three branches into `main` in any order and confirm no frozen file changed outside the Phase 0 commit (`git diff --stat` against the base commit for `src/app/**`, `src/store/**`, `src/lib/**`, `src/features/tables/TableView.tsx`). **Done when**: the merge is conflict-free and `npx tsc --noEmit` plus `npm run build` pass on `main`. (S)
- [ ] 4.2 Run the RF-01..RF-35 smoke checklist on the Vercel production deploy in desktop Chrome, using `docs/requirements.md` (read-only) section 4 as the ID list: event lifecycle (RF-01..03), templates and tables (RF-04..13), guests (RF-14..19), seating (RF-20..26), persistence (RF-27). **Done when**: every one of those 27 IDs is ticked or has a recorded defect. (M)
- [ ] 4.3 Verify the share and export chain end to end on the deploy: copy the link, open it in a private window, search a duplicated name, and export the PNG (RF-28..RF-33, RF-35). **Done when**: the link decodes with no editor data leaking in, both duplicates are listed, the right seat is highlighted, and the PNG downloads complete. (S)
- [ ] 4.4 RF-34 mobile check: open the shared link on a real phone (or 360x640 emulation) and complete a search-and-select. **Done when**: no horizontal scrolling, the table drawing is legible, and the match is selectable by tap. (S)
- [ ] 4.5 Scale check: share a ~60-guest event (QR path) and a ~200-guest event (copy-link fallback path) from the deploy. **Done when**: the 60-guest link renders a scannable QR, the 200-guest dialog renders the note and copies without crashing, and both links open correctly in the guest view. (S)
